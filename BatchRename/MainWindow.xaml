<Fluent:RibbonWindow 
        x:Class="BatchRename.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BatchRename"
        xmlns:Fluent="clr-namespace:Fluent;assembly=Fluent"
        mc:Ignorable="d"
        Loaded="RibbonWindow_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <Fluent:Ribbon>
            <Fluent:Ribbon.Menu>
                <Fluent:Backstage Header="File"/>
            </Fluent:Ribbon.Menu>
            <Fluent:RibbonTabItem Header="Home">
                <Fluent:RibbonGroupBox Header="Basic">
                    <Fluent:Button x:Name="btnAdd" Width="50" Header="Add" Click="btnAdd_Click">
                        <Fluent:Button.LargeIcon>
                            <Image Source="Images/add.png" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Fluent:Button.LargeIcon>
                    </Fluent:Button>
                    <Fluent:Button x:Name="btnRemove" Width="50" Header="Remove" >
                        <Fluent:Button.LargeIcon>
                            <Image Source="Images/remove.png" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Fluent:Button.LargeIcon>
                    </Fluent:Button>
                    <Fluent:Button x:Name="btnClearAll" Width="50" Header="Clear All">
                        <Fluent:Button.LargeIcon>
                            <Image Source="Images/clearAll.png" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Fluent:Button.LargeIcon>
                    </Fluent:Button>
                </Fluent:RibbonGroupBox>
                <Fluent:RibbonGroupBox Header="Tool">
                    <Fluent:Button x:Name="btnReplace" Width="65" Header="Replace">
                        <Fluent:Button.LargeIcon>
                            <Image Source="Images/replace.png" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Fluent:Button.LargeIcon>
                    </Fluent:Button>
                    <Fluent:Button x:Name="btnNewCase" Width="65" Header="New Case" >
                        <Fluent:Button.LargeIcon>
                            <Image Source="Images/newcase.png" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Fluent:Button.LargeIcon>
                    </Fluent:Button>
                    <Fluent:Button x:Name="btnNormalize" Width="65" Header="Normalize">
                        <Fluent:Button.LargeIcon>
                            <Image Source="Images/normalize.png" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Fluent:Button.LargeIcon>
                    </Fluent:Button>
                    <Fluent:Button x:Name="btnRandomName" Width="65" Header="Random Name">
                        <Fluent:Button.LargeIcon>
                            <Image Source="Images/random.png" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Fluent:Button.LargeIcon>
                    </Fluent:Button>
                </Fluent:RibbonGroupBox>
            </Fluent:RibbonTabItem>
        </Fluent:Ribbon>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="600"></ColumnDefinition>
                <ColumnDefinition Width="200"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Border CornerRadius="0,0,20,20" Grid.Column="0"  BorderBrush="Red" BorderThickness="2" >
                <ListView x:Name="listFiles" Height="auto"  Background="Transparent"  VerticalAlignment="Top"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      Grid.Column="0">
                
                <ListView.View >
                    <GridView>
                        <GridViewColumn Header="Name" Width="150" DisplayMemberBinding="{Binding Name}"></GridViewColumn>
                        <GridViewColumn Header="New Name" Width="150" DisplayMemberBinding="{Binding NewName}"></GridViewColumn>
                        <GridViewColumn Header="Path" Width="150" DisplayMemberBinding="{Binding Path}"></GridViewColumn>
                        <GridViewColumn Header="Error" Width="150" DisplayMemberBinding="{Binding Error}"></GridViewColumn>
                    </GridView>
                </ListView.View>
                <ListView.Resources>
                    <ContextMenu x:Key="ItemContextMenu">
                        <MenuItem Header="Delete" ></MenuItem>
                        <MenuItem Header="Update" ></MenuItem>
                    </ContextMenu>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="15"/>
                        <Setter Property="Background" Value="Red"/>
                        <Setter Property="BorderBrush" Value="Transparent"></Setter>
                    </Style>
                </ListView.Resources>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="{x:Type ListViewItem}">
                            <Setter Property="ContextMenu" Value="{StaticResource ItemContextMenu}"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel></WrapPanel>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
            </Border>
            <Border CornerRadius="0,0,20,20" Grid.Column="1" BorderBrush="Red" BorderThickness="2" Margin="10" >
            <ListView Name="listChoice" Background="Transparent" Height="200" Grid.Column="1">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Changed" Width="170"></GridViewColumn>
                    </GridView>
                </ListView.View>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Transparent" Height="35">
                            <TextBlock TextWrapping="Wrap"
                                       Text="{Binding}"
                                   FontSize="10"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Foreground="Blue"
                                       FontWeight="Bold"/>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            </Border>
        </Grid>
    </StackPanel>
</Fluent:RibbonWindow>
